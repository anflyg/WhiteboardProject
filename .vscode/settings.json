{
  // Use project venv for Python in VS Code (run/debug and linting)
  "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python",
  // Auto-activate environments in the integrated terminal
  "python.terminal.activateEnvironment": true
}
